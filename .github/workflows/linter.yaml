name: Lint

on: push

jobs:
  lint:
    runs-on: ubuntu-22.04
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: Install tools
        run: sudo apt-get --yes install just shellcheck shfmt
      - name: Run makefile
        run: just
